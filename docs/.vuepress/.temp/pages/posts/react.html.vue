<template><div><p>1.react 语法</p>
<ul>
<li>生命周期</li>
<li>jsx</li>
<li>view  user  state store</li>
</ul>
<p>2.typescript 语法</p>
<ul>
<li>interface?</li>
<li>enum?</li>
<li>泛型 ？ 断言？</li>
</ul>
<p>3.reactHooks 语法</p>
<p>4.组件</p>
<p>5.路由</p>
<p>6.redux</p>
<p>:happy:</p>
<p>7.钩子函数</p>
<ul>
<li><strong>useState</strong></li>
</ul>
<p>使用useState的方式很简单，首先需要在你的组件中引入useState，然后就可以在你的组件中使用它了： const [state, setState] = useState(initialState);</p>
<p>在这里，你可以将state定义为你想要的任何状态，比如一个布尔值或者一个对象。 setState可以用来更新state的值，比如：</p>
<p>setState(!state);</p>
<p>当你的组件重新渲染时，state的值将会更新为新值</p>
<ul>
<li><strong>useEffect</strong></li>
</ul>
<p>useEffect用于在渲染期间运行副作用操作，例如订阅、网络请求等。它接收两个参数，第一个参数是一个函数，它将在组件第一次渲染后（或更新后）立即调用，第二个参数是一个数组，它告诉React在何时跳过effect的执行，只有当数组中的变量更改时才会重新执行。</p>
<blockquote>
<p>hhh</p>
<p>hhh</p>
</blockquote>
<table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody>
<tr>
<td>| ---- | ----|</td>
<td></td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>hh</th>
<th>hh</th>
</tr>
</thead>
<tbody>
<tr>
<td></td>
<td></td>
</tr>
</tbody>
</table>
</div></template>


